machine:
  node:
    version: v6.1.0

general:
  build_dir: development

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - nvm ls-remote
      - echo "{\"version\":\"$CIRCLE_TAG\"}" > ./version.json
      - make prod publish
